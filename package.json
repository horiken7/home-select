{
  "name": "home-select",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:worker": "wrangler deploy",
    "dev:worker": "wrangler dev",
    "scrape:properties": "node scripts/scrape-properties.mjs"
  },
  "devDependencies": {
    "playwright": "latest",
    "wrangler": "latest"
  }
}
